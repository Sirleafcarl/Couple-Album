# Public snapshot notes

This repository starts with a new root commit using a GitHub no-reply identity. Private development history is not included.

Excluded: personal media/storage, database contents and backups, actual environment files, logs and reports, internal planning notes, private local paths, prototype hosting metadata, and the original sacred-joy character assets.

The public sacred-joy background and thumbnail use the generated sky-letters artwork as a neutral placeholder. Theme IDs and application behavior are unchanged. Local private development files are not replaced by this snapshot.

Only four generated design-preview photos used by browser tests are retained under `docs/design-preview/public/assets/`; the standalone design-preview app is not shipped. Runtime botanical backgrounds are retained separately with provenance notes.

Before publishing future updates, repeat the private-data and asset review. Do not merge private development history into this public history, and do not copy a real `.env` or data directory into this repository.

Deployment update: includes production Docker/Compose, photo trash and viewer improvements, PWA support and the existing generated Kitty themes. The cancelled fairytale castle runtime and preview screenshots are excluded; its database migration is retained for forward-history compatibility. Deployment addresses are examples, passwords remain empty, and internal planning notes are excluded. The original sacred-joy assets remain replaced with the neutral public placeholder.
