# Botanical material

`story-paper.png` and `mobile-paper.png` are copied from the approved local design preview. Both were generated with Image Gen for this project; no user photographs are included. Provenance is recorded in `docs/design-preview/ASSETS.md`.

They are rendered as fixed, non-interactive decoration. Album photos are loaded separately from authenticated media endpoints.
